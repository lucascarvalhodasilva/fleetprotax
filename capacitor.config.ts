import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carvalhodasilva.fleetprotax',
  appName: 'FleetProTax',
  webDir: 'out'
};

export default config;
